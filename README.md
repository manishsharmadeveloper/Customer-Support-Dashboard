Step-1 
Clone Repo and open in Code Editor & git clone

Step-2
    1. npm install

Step-3
  1. Create a .env file in your root folder
  2. paste into .env file VITE_API_URL=http://localhost:3000/tickets

Step-4
  1. npm run dev

Step-5
  1. npm install -D json-server
  2. npx json-server db.json --port 3000
